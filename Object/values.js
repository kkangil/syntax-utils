/**
 * Object​.values()
 * Object​.keys 가 키값을 리턴해주는 메소드라면 values는 값을 리턴
 * 
 * node >= 7.0.0 / IE x
 */
const obj = { 0: 'a', 1: 'b', 2: 'c' };
console.log(Object.keys(obj)); // console: ['a', 'b', 'c']